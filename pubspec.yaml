name: zerogrant_package_widgets
description: "A new Flutter package project."
version: 0.0.2
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  delightful_toast: ^1.1.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_gen: ^5.8.0
  build_runner: ^2.4.13

flutter:
  generate: true
